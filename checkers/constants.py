# add all the constants numbers and data here
import pygame

WIDTH, HEIGHT = 500, 500      # height and width of the game board

ROWS, COLS = 8, 8            # rows and col of the game
SQUARE_SIZE = WIDTH//COLS    # how big is one square size

# rgb colors
RED = (255, 0, 0)
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
BLUE = (0, 0, 255 )
GREY = (119, 136, 153)

# CROWN = pygame.transform.scale(pygame.image.load(''), (44, 25))
